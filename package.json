{
  "name": "team-generator",
  "version": "1.0.0",
  "description": "A command line application that generates a development team page based on user inputs",
  "scripts": {
    "test": "jest --watch"
  },
  "author": "Robert Rousset",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "dependencies": {
    "inquirer": "^8.0.0",
    "prettier": "^2.3.0"
  }
}
